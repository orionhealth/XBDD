import { makeStyles, createStyles } from '@material-ui/core';

export const useBuildListStyles = makeStyles(() =>
  createStyles({
    versionsSelector: {
      paddingTop: '16px',
      paddingBottom: '16px',
    },
  })
);

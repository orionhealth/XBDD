import { makeStyles, createStyles } from '@material-ui/core';

export const useBuildSummaryStyles = makeStyles(() =>
  createStyles({
    versionsSelector: {
      paddingTop: '16px',
      paddingBottom: '16px',
    },
  })
);

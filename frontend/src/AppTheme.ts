import { createMuiTheme } from '@material-ui/core';

const AppTheme = createMuiTheme({
  palette: {
    primary: {
      main: '#457B9D',
      contrastText: '#fefefe',
    },
    secondary: {
      main: '#fefefe',
    },
  },
});

export default AppTheme;

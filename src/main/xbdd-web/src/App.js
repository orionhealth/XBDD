import React from 'react';
import { ReactDOM } from 'react-dom';
import Xbdd from './Xbdd';

// eslint-disable-next-line
("use strict");

const domContainer = document.querySelector('#xbb-react-app');
ReactDOM.render(React.createElement(Xbdd), domContainer);
